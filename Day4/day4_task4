#include <stdio.h>

#define MAX 10

enum Status {
    NEVOJITET_PUNE = 1,
    MESATAR,
    SHKELQYESHEM
};

struct Student {
    int id;
    char name[30];
    float progress;
    enum Status status;
};

// SHFAQJE
void showStudent(struct Student *s) {
    printf("\nID: %d\n", s->id);
    printf("Emri: %s\n", s->name);
    printf("Progresi: %.2f\n", s->progress);

    if (s->status == NEVOJITET_PUNE)
        printf("Status: Nevojitet pune\n");
    else if (s->status == MESATAR)
        printf("Status: Mesatar\n");
    else
        printf("Status: Shkelqyeshem\n");
}

// 🔥 FUNKSIONI I RI (ME POINTER)
// përditëson progresin dhe statusin
void updateProgress(struct Student *s) {
    float newProgress;

    printf("Shkruani progresin e ri: ");
    scanf("%f", &newProgress);

    s->progress = newProgress; // ndryshon direkt te studenti

    // rifreskon statusin
    if (newProgress < 50)
        s->status = NEVOJITET_PUNE;
    else if (newProgress < 80)
        s->status = MESATAR;
    else
        s->status = SHKELQYESHEM;

    printf("Progresi u perditesua me sukses!\n");
}

int main() {
    struct Student students[MAX];
    int count = 0;
    int choice;

    do {
        printf("\n===== MENU =====\n");
        printf("1. Shto student\n");
        printf("2. Shfaq studentet\n");
        printf("3. Perditeso progres\n"); // OPSION I RI
        printf("4. Dil\n");
        printf("Zgjedhja: ");
        scanf("%d", &choice);

        switch(choice) {

            case 1:
                if (count >= MAX) {
                    printf("Kapaciteti eshte i mbushur!\n");
                    break;
                }

                printf("ID: ");
                scanf("%d", &students[count].id);

                printf("Emri: ");
                scanf("%s", students[count].name);

                printf("Progresi: ");
                scanf("%f", &students[count].progress);

                if (students[count].progress < 50)
                    students[count].status = NEVOJITET_PUNE;
                else if (students[count].progress < 80)
                    students[count].status = MESATAR;
                else
                    students[count].status = SHKELQYESHEM;

                count++;
                break;

            case 2:
                if (count == 0) {
                    printf("Nuk ka te dhena!\n");
                } else {
                    for (int i = 0; i < count; i++) {
                        showStudent(&students[i]);
                    }
                }
                break;

            case 3: {
                if (count == 0) {
                    printf("Nuk ka studente!\n");
                    break;
                }

                int idSearch;
                printf("Shkruani ID e studentit: ");
                scanf("%d", &idSearch);

                int found = 0;

                for (int i = 0; i < count; i++) {
                    if (students[i].id == idSearch) {
                        updateProgress(&students[i]); // POINTER KRYESOR
                        found = 1;
                        break;
                    }
                }

                if (!found) {
                    printf("Studenti nuk u gjet!\n");
                }

                break;
            }

            case 4:
                printf("Duke dale...\n");
                break;

            default:
                printf("Zgjedhje e pavlefshme!\n");
        }

    } while (choice != 4);

    return 0;
}
